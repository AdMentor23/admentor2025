// Create a new file: projects.js
document.addEventListener('DOMContentLoaded', function() {
    // Initialize AOS for animations
    AOS.init({
        duration: 800,
        once: true,
        offset: 100
    });

    // Add any project-specific functionality here
}); 